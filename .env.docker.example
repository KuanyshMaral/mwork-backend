PORT=8080
ENV=development

# внутри docker-compose хост БД = postgres (имя сервиса)
DATABASE_URL=postgresql://mwork:mwork_secret@postgres:5432/mwork_dev?sslmode=disable


# внутри docker-compose хост Redis = redis (имя сервиса)
REDIS_URL=redis://redis:6379/0

JWT_SECRET=CHANGE_ME_TO_LONG_RANDOM_SECRET
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
LOG_LEVEL=debug

# PhotoStudio integration
PHOTOSTUDIO_BASE_URL=http://photostudio:8090
PHOTOSTUDIO_TOKEN=
PHOTOSTUDIO_SYNC_ENABLED=false
PHOTOSTUDIO_TIMEOUT_SECONDS=10
