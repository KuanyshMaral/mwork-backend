# Пример .env для логирования

# ========== LOGGING CONFIGURATION ==========

# Log level: debug, info, warn, error, fatal
LOG_LEVEL=debug

# Environment: development, production, test
ENV=development

# Optional: Path to log file (logs will be written to both stdout and file)
# LOG_FILE=./logs/api.log

# ========== DEVELOPMENT SETTINGS ==========
# For development:
# - Logs are printed to console with nice formatting
# - Log level is usually debug to see everything
# - Set ENV=development to enable pretty console output

# ========== PRODUCTION SETTINGS ==========
# For production:
# - Set LOG_LEVEL=info or higher
# - Set ENV=production for JSON format
# - Set LOG_FILE to persist logs to disk
# - Example production config:
# LOG_LEVEL=info
# ENV=production
# LOG_FILE=/var/log/mwork-api/app.log
