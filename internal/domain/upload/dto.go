package upload

// dto.go is intentionally removed.
// All DTO types are now inline in handler.go (uploadResponse struct).
// This file is preserved as an empty placeholder to avoid compilation issues
// if any external tools reference the package.
